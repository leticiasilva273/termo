export interface Usuario { id:number; username:string; nome:string; senha:string; }
export interface Ranking { nome:string; username:string; melhor_sequencia:number; total_erros:number; palavras_acertadas:number; }
